import { NavBar } from "../navbar";

export function Header() {
	return (
		<div className="fixed left-0 right-0 top-0 z-20 flex h-14 items-center justify-center gap-2 overflow-hidden border-b border-primary bg-hover px-2 pt-1"></div>
	);
}
